- name: Demo use of my_own_namespace.yandex_cloud_elk collection
  hosts: localhost
  gather_facts: false
  connection: local

  vars:
    my_module_path: "/tmp/demo_config.txt"
    my_module_content: |
      # Demo configuration
      environment=production
      log_level=info

  roles:
    - role: my_own_namespace.yandex_cloud_elk.use_my_module
