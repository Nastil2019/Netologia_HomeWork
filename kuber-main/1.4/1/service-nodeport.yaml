apiVersion: v1
kind: Service
metadata:
  name: multi-container-nodeport
spec:
  selector:
    app: multi-container
  ports:
  - name: nginx
    port: 80
    targetPort: 80
    nodePort: 30080
  type: NodePort